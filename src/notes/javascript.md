---
title: Javascript
tags: [coding, javascript]
layout: layout.njk
---
responsive design - adjust websites for diff. types of devices; made up of 3 practises:
media grids- 3 types fixed grids flexible and hybrid
fluid images-
media queries-
import libaries  with link tag
---BOOTSTRAP---
<div class="container"> make new container 
<div class="row"> make new row
<div class="col"> make new column 
class="img-fluid" - make images fluid
<table class="table"> new table
breakpoint - extra small (<576px) small/sm (576-767px) medium/md (768-991px), large/lg (992-1199) extra large/xl (1200-1399) extra extralarge/xxl(1400px+) extra small is default
modifiers - primary, secondary, success, info, warning, danger, light,dark. tweaks types
eg. <class="container">
<div class-"row">
<div class="col-lg-6">
div class="alert alert-primary" role="alert">
close tages here
bllotstrap uses 12 columns that can be grouped with -[num of columns] eg. col-lg-6 you can also do something like "col-12 col-lg-4"

 prossess request to make new content

dynamic contant - diff for each user(needs to)
static content - same for each user
aplication server - server for displaying dynamic content gets info from web server
SPAs (single page aplications)-page will rewright as user uses it instead of sending to a new page
bundling - all code at oncce (slow)
lazy loading - ges info as needed

react - good for reusing code and for SPAs; uses components (snipets to reuse)
