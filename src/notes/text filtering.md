---
title: text filtering 
tags: [terminal, linux, cheat sheet]
layout: layout.njk
---
cut -d "____" -f # - cut all data after # instances of string entered in and before previouse instance of string entered in.
example:
![example](/images/image-1.png)

______ | grep “       ” - search for a string in a comand that returns text or a .txt file and only return that line with the word(s) and the word is in red.

tr -d “___” - delete all instances of given string 